# AGENT.md - RPChat Development Guide

## Architecture
- **Frontend**: Single-file HTML/CSS/JS in `index.html` - editable AI chat interface with Dark Mode support
- **Config**: JSON files for providers (`providers.json`), system prompts (`systemprompts.json`), and user Preferences in `localStorage`

## Code Style
- **HTML/CSS/JS**: Standard formatting, semantic HTML, modern CSS, vanilla JavaScript
- **JSON**: Well-formatted configuration files with clear structure
- **Comments**: Minimal inline comments, focus on self-documenting code
